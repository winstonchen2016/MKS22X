public class Merge{

    public Merge(){};

    public static void mergesort(int[] ary){
	if(ary.length <= 1){
	    return;
	}
	else{
	    int half = ary.length/2
	    int[] a = copyOfRange(
	
	merge(ary, 0, h);
    }
}
